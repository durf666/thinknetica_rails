%p#notice= notice
%h1 Listing Trains
%table
  %thead
    %tr
      %th Number
      %th{:colspan => "3"}
  %tbody
    - @trains.each do |train|
      =render train
%br/
= link_to 'New Train', new_train_path